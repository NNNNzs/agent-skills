# 故障排除

## Python 未安装

确保系统已安装 Python 3：
```bash
python3 --version  # 应该 >= 3.6
```

## 脚本无法执行

确保脚本有执行权限：
```bash
chmod +x scripts/*.py
```

## URL 无法访问

1. 检查 URL 是否正确
2. 检查是否需要认证
3. 尝试使用代理或下载到本地

## 文档太大

不要尝试读取整个文档，使用分块查询：
- 先获取 tags
- 再按 tag 获取接口列表
- 最后获取具体接口详情
